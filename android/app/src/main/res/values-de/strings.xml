<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Shown when an error occurs while loading the app -->
    <string name="offline_heading">Du bist offline.</string>

    <!-- Button to retry loading the app after an error -->
    <string name="offline_description">Tippe hier, um es nochmal zu probieren!</string>

    <!-- Shown when the app tries to use a feature that needs an external app, but the app is not found -->
    <string name="noapp_heading">App nicht gefunden.</string>

    <!-- Shown when the app tries to use a feature that needs an external app, but the app is not found -->
    <string name="noapp_description">Sorry, du hast keine App, die diese Aktion durchführen kann.</string>

    <!-- Shown when the user tries to upload a photo for a place -->
    <string name="camera_access_required">Bitte erlaube Kamera-Zugriff, um ein Foto hinzuzufügen.</string>

    <!-- Button caption used in multiple places -->
    <string name="ok">Okay</string>

    <!-- Shown when the device has no built-in camera, but the user tries to add a place photo -->
    <string name="device_has_no_camera">Sorry, Fotos hinzufügen funktioniert nur auf Geräten mit einer Kamera.</string>

    <!-- Shown when a storage problem occurs while taking a place photo -->
    <string name="cannot_generate_image_tempfile">Sorry, konnte das Bild nicht speichern. Hast Du genug Speicher?</string>

    <!-- Shown when the user cancels the camera while adding a place photo -->
    <string name="no_image_uploaded">Kein Bild hochgeladen.</string>
</resources>
