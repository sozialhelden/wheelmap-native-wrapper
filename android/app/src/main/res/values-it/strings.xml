<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Shown when an error occurs while loading the app -->
    <string name="offline_heading">Sei offline.</string>

    <!-- Button to retry loading the app after an error -->
    <string name="offline_description">Tocca qui per riprovare!</string>

    <!-- Shown when the app tries to use a feature that needs an external app, but the app is not found -->
    <string name="noapp_heading">App non trovata</string>

    <!-- Shown when the app tries to use a feature that needs an external app, but the app is not found -->
    <string name="noapp_description">Nessuna app su questo dispositivo può portare a termine l\'operazione.</string>

    <!-- Shown when the user tries to upload a photo for a place -->
    <string name="camera_access_required">Per aggiungere una foto, autorizza l\'accesso alla fotocamera.</string>

    <!-- Button caption used in multiple places -->
    <string name="ok">Okay</string>

    <!-- Shown when the device has no built-in camera, but the user tries to add a place photo -->
    <string name="device_has_no_camera">Mi dispiace, l\'aggiunta di foto funziona soltanto su dispositivi dotati di fotocamera.</string>

    <!-- Shown when a storage problem occurs while taking a place photo -->
    <string name="cannot_generate_image_tempfile">Mi dispiace, non riesco a salvare l\'immagine. Hai spazio a sufficienza?</string>

    <!-- Shown when the user cancels the camera while adding a place photo -->
    <string name="no_image_uploaded">Nessuna immagine caricata.</string>
</resources>
